services:
  cronbird:
    build:
      context: .
    container_name: cronbird
    restart: unless-stopped
    ports:
      - "8080:8080"
    env_file: .env
    volumes:
      - cronbird-data:/data

volumes:
  cronbird-data:
    driver: local
